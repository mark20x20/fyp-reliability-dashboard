# copy to .env and adjust
SEED=42
N_RUNS=30
DROPOUT_P=0.2
DROPOUT_LAYERS=layer2,layer3

DATA_ROOT=data
OUTPUT_ROOT=outputs
DB_PATH=db/reliability.db
CHECKPOINT_DIR=models/checkpoints

IOU_PERCENTILE=80
TOPK_K=0.10
INPUT_SIZE=224
BATCH_SIZE=64

STREAMLIT_PORT=8501
CUDA_VISIBLE_DEVICES=0
